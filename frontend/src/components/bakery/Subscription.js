import React from 'react';

const Subscription = (props) => {
  return(
    <div className="Subscription-container" >

      <h2>{props.subscription.name}</h2>
      
      </div>
  )
}

export default Subscription;